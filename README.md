# vsb-api

Media sequence management service.

## Setup

1. Copy `.env.example` → `.env` and fill values
2. `pip install -r requirements.txt`
3. `python bot.py`

## Deploy

Push to GitHub (private repo recommended) → connect to Render → set env vars in dashboard.
